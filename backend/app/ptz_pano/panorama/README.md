# Panorama Compatibility

Compatibility import path for older code.

New stitching implementation lives in:

```text
backend/app/ptz_pano/stitching/
```

Keep this package thin. New panorama assembly work should go into `stitching`.
